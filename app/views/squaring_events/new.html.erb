<!-- group.users.each do |member|
	"member.first_name member.last_name: member.email"
end

group.squared_transactions.each do |transaction|
	"#{transaction.debtor.first_name} owes #{transaction.creditor.first_name} $#{transaction.amount}!"
end

link_to "Approve", /users/:user_id/groups/:group_id/squaring_events, method: :post
link_to "Cancel", users_path -->
